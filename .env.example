# Copy to .env.local for local-only overrides.
# Keep committed env files free of secrets.

APP_ENV=local
HTTP_ADDR=:8080
SERVICE_NAME=agentic-polyglot-starter
PUBLIC_APP_NAME=Agentic Polyglot Starter
